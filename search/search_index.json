{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Loom \ud83e\uddf5","text":"<p><code>loom</code> is a high-performance, graph-based AI workflow engine for Go. It allows you to build complex agentic systems by defining multi-step workflows as directed graphs, seamlessly connecting them to any large language model, and persisting execution state for long-running or interrupted tasks.</p> <p>Unlike linear chains, <code>loom</code> provides a robust, state-first architecture inspired by state machines, making it easy to build agents that can loop, branch, and resume exactly where they left off.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>\ud83d\udd78\ufe0f Graph-based Workflows: Define agents as directed graphs with nodes and conditional edges.</li> <li>\ud83d\udcbe State Persistence: Built-in checkpointing with support for PostgreSQL and SQLite.</li> <li>\ud83d\udcfa Real-time Streaming: First-class support for token-level streaming and event-based updates.</li> <li>\ud83e\udde0 Advanced Memory Management: Intelligently condense long conversations and trim message history.</li> <li>\ud83d\udd0c Provider Agnostic: One interface for OpenAI, Anthropic, Ollama, and Google Gemini.</li> <li>\ud83d\udd0c MCP Support: Native integration with the Model Context Protocol for tools, resources, and prompts.</li> <li>\ud83d\udee0\ufe0f Tool Integration: Native support for tool calling and tool-use loops.</li> <li>\ud83d\udee1\ufe0f Type Safe: Built from the ground up with Go generics for maximum developer productivity.</li> </ul>"},{"location":"#why-loom","title":"Why Loom?","text":"<p>Loom is designed for developers who need more control than what simple chains or \"black-box\" agent frameworks offer. By treating your AI workflow as a state machine, Loom gives you:</p> <ol> <li>Explicit Control: You define exactly how the state transitions between nodes.</li> <li>Durability: Every step is checkpointed, so your agents can survive crashes or wait for human input.</li> <li>Observability: Built-in tools to visualize your graphs and trace every token and tool call.</li> </ol> <p>Get Started with the Quick Start Guide \u2192</p>"},{"location":"installation/","title":"Installation \ud83d\udce6","text":"<p>Loom is a Go library. To include it in your project, use <code>go get</code>.</p>"},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Go: 1.23 or higher is recommended (Loom uses generics and modern Go features).</li> <li>Environment: Linux, macOS, or Windows.</li> </ul>"},{"location":"installation/#basic-installation","title":"Basic Installation","text":"<pre><code>go get github.com/masterkeysrd/loom\n</code></pre>"},{"location":"installation/#llm-providers","title":"LLM Providers","text":"<p>Depending on which LLM providers you intend to use, you may need to set specific environment variables for authentication.</p> Provider Requirement OpenAI <code>OPENAI_API_KEY</code> Anthropic <code>ANTHROPIC_API_KEY</code> Google Gemini <code>GOOGLE_API_KEY</code> Ollama Local Ollama instance running"},{"location":"installation/#persistence-backends","title":"Persistence Backends","text":"<p>Loom supports SQLite and PostgreSQL for checkpointing. These are optional but required if you want state persistence.</p>"},{"location":"installation/#sqlite","title":"SQLite","text":"<p>Requires the <code>github.com/mattn/go-sqlite3</code> driver.</p> <pre><code>go get github.com/mattn/go-sqlite3\n</code></pre>"},{"location":"installation/#postgresql","title":"PostgreSQL","text":"<p>Requires a standard PostgreSQL connection string.</p>"},{"location":"installation/#development-setup","title":"Development Setup","text":"<p>If you plan to contribute to Loom or run the documentation locally, you can use the provided <code>Makefile</code> to simplify the setup.</p> <pre><code># Install all development dependencies (Go modules and MkDocs)\nmake install\n\n# Start the documentation server\nmake docs-serve\n</code></pre> <p>Available commands: - <code>make generate</code>: Run code generation for LLM profiles. - <code>make test</code>: Run all tests with race detection. - <code>make clean</code>: Cleanup logs and temporary files.</p> <p>Once installed , proceed to the Quick Start Guide to build your first agent!</p>"},{"location":"guides/conversations/","title":"Conversations &amp; Messages \ud83d\udcac","text":"<p>In Loom, a conversation is represented as an ordered list of messages. Because Loom is \"state-first,\" these messages are typically stored in your <code>GraphState</code> and passed to LLMs to provide context.</p>"},{"location":"guides/conversations/#1-message-roles","title":"1. Message Roles","text":"<p>Loom defines four core roles, each representing a different participant in the conversation:</p> Role Description Concrete Type System High-level instructions for the model (e.g., \"You are a helpful assistant\"). <code>message.System</code> User Messages sent by the human user. <code>message.User</code> Assistant Responses generated by the AI model. <code>message.Assistant</code> Tool The result of an external tool execution. <code>message.Tool</code>"},{"location":"guides/conversations/#creating-messages","title":"Creating Messages","text":"<pre><code>import \"github.com/masterkeysrd/loom/message\"\n\nsystemMsg := message.NewSystemText(\"You are a math tutor.\")\nuserMsg   := message.NewUserText(\"What is the square root of 144?\")\n</code></pre>"},{"location":"guides/conversations/#2-content-blocks","title":"2. Content Blocks","text":"<p>Loom messages are composed of one or more Blocks. This flexible structure supports multimodal inputs and advanced features like \"chain-of-thought\" thinking.</p>"},{"location":"guides/conversations/#text-blocks","title":"Text Blocks","text":"<p>The most common block type, used for simple text.</p> <pre><code>block := &amp;message.TextBlock{Text: \"Hello!\"}\n</code></pre>"},{"location":"guides/conversations/#multimodal-blocks-images-documents","title":"Multimodal Blocks (Images, Documents)","text":"<p>You can attach images, PDFs, or other media to a user message.</p> <pre><code>imageMsg := message.NewUserImage(imageData, \"image/png\")\n// OR via URL\nimageMsg := message.NewUserImageURL(\"https://example.com/photo.jpg\")\n</code></pre>"},{"location":"guides/conversations/#thinking-blocks","title":"Thinking Blocks","text":"<p>Some models (like Claude 3.5 or Gemini 2.0) can return their internal reasoning. Loom surfaces this as a <code>ThinkingBlock</code>.</p> <pre><code>// Access the thinking part of an assistant response\nthinking := assistantMsg.Content.Thought()\n</code></pre>"},{"location":"guides/conversations/#3-managing-history-messagelist","title":"3. Managing History (MessageList)","text":"<p>A <code>MessageList</code> is a specialized slice of messages that provides critical functionality for state management and persistence. Always use <code>MessageList</code> instead of a raw <code>[]message.Message</code> when storing history in your Graph State.</p> <pre><code>type MyState struct {\n    History message.MessageList `json:\"history\"`\n}\n</code></pre>"},{"location":"guides/conversations/#why-use-messagelist","title":"Why use MessageList?","text":"<ol> <li>Polymorphic Serialization: <code>MessageList</code> automatically handles the complex task of serializing different message types (User, Assistant, etc.) into a single JSON array with a \"role\" discriminator.</li> <li>Type-Safe Deserialization: When loading a checkpoint from a database (SQL or PG), <code>MessageList</code> ensures that raw JSON is correctly restored into the proper Go structs.</li> <li>Fluent Helpers: Provides methods like <code>Last()</code> and <code>Text()</code> to easily access content without range loops.</li> </ol> <pre><code>history := message.MessageList{\n    message.NewSystemText(\"...\"),\n    message.NewUserText(\"...\"),\n}\n\n// Add a new response\nhistory = append(history, assistantResponse)\n</code></pre>"},{"location":"guides/conversations/#4-token-metrics","title":"4. Token Metrics","text":"<p>Loom tracks token usage for every assistant response. This information is attached to the <code>message.Assistant</code> struct.</p> <pre><code>if resp.Metrics != nil {\n    fmt.Printf(\"Input: %d, Output: %d, Total: %d, Estimated Cost: %s\\n\",\n        resp.Metrics.Tokens.Input,\n        resp.Metrics.Tokens.Output,\n        resp.Metrics.TotalTokens,\n        resp.Metrics.TotalCost)\n}\n</code></pre>"},{"location":"guides/conversations/#5-message-extensions-metadata","title":"5. Message Extensions &amp; Metadata","text":"<p>Loom provides two ways to attach auxiliary information to a message:</p>"},{"location":"guides/conversations/#extensions-type-safe-hints","title":"Extensions (Type-Safe Hints)","text":"<p>Extensions are provider-specific structs that influence model behavior. Unlike raw metadata, Extensions are type-safe and are automatically restored to their concrete types when loaded from storage.</p> <p>Loom provides two methods for adding extensions:</p> <ol> <li><code>WithExtension</code> (Fluent): Available on concrete message types (<code>User</code>, <code>Assistant</code>, etc.). It returns the same concrete type, allowing for method chaining without type erasure.</li> <li><code>AddExtension</code> (Polymorphic): Available on the <code>Message</code> interface. Use this when you are working with a generic message object.</li> </ol> <pre><code>// 1. Fluent usage (preserves *message.User type)\nmsg := message.NewUserText(\"Analyze this...\").\n    WithExtension(&amp;loomanthropic.MessageCache{Enabled: true})\n\n// 2. Interface usage\nvar m message.Message = msg\nm.AddExtension(&amp;someother.Extension{})\n</code></pre>"},{"location":"guides/conversations/#metadata-arbitrary-data","title":"Metadata (Arbitrary Data)","text":"<p>Metadata is a simple <code>map[string]any</code> for application-level data (IDs, timestamps, custom flags) that doesn't need a formal schema.</p> <pre><code>msg.Metadata[\"session_id\"] = \"xyz-123\"\n</code></pre>"},{"location":"guides/conversations/#summary","title":"Summary","text":"<ul> <li>Roles: Use <code>System</code>, <code>User</code>, <code>Assistant</code>, and <code>Tool</code> to distinguish participants.</li> <li>Blocks: Mix and match <code>TextBlock</code>, <code>ImageBlock</code>, and <code>ThinkingBlock</code> for rich interactions.</li> <li>MessageList: Use this type to manage and persist your conversation history in your graph state.</li> </ul>"},{"location":"guides/custom-providers/","title":"Custom Providers (Advanced) \ud83d\udee0\ufe0f","text":"<p>Loom is designed to be provider-agnostic. While it comes with built-in support for OpenAI, Anthropic, Gemini, and Ollama, you can easily add support for any other LLM backend by implementing the <code>llm.Provider</code> interface.</p>"},{"location":"guides/custom-providers/#1-the-provider-interface","title":"1. The Provider Interface","text":"<p>To create a custom provider, you must implement the following interface:</p> <pre><code>type Provider interface {\n    Name() string\n    Stream(context.Context, *Request) (StreamResponse, error)\n    ListProfiles() []ModelProfile\n    GetProfile(id string) (ModelProfile, bool)\n}\n</code></pre>"},{"location":"guides/custom-providers/#optional-cache-management","title":"Optional: Cache Management","text":"<p>If your provider supports explicit resource management for context caching, you can optionally implement the <code>CacheManager</code> interface:</p> <pre><code>type CacheManager interface {\n    CreateCache(context.Context, *Request) (string, error)\n    DeleteCache(context.Context, string) error\n}\n</code></pre>"},{"location":"guides/custom-providers/#optional-extensions","title":"Optional: Extensions","text":"<p>If your provider has specialized features, you should define your own extension structs.</p> <ol> <li>Define the struct: It must implement either <code>llm.Extension</code> or <code>message.Extension</code>.</li> <li>Implement <code>ExtensionID()</code>: Return a unique string (e.g. <code>myprovider.feature</code>).</li> <li>Register (Message level only): If it's a message-level extension, call <code>message.RegisterExtension</code> in your <code>init()</code> function so it can be restored from JSON.</li> </ol> <pre><code>func init() {\n    message.RegisterExtension(func() message.Extension { return &amp;MyMsgExt{} })\n}\n</code></pre>"},{"location":"guides/custom-providers/#2-implementing-the-stream-method","title":"2. Implementing the Stream Method","text":"<p>The core of a provider is the <code>Stream</code> method. It takes a generic <code>llm.Request</code> and returns an iterator over <code>message.AssistantChunk</code> values.</p> <pre><code>func (p *MyCustomProvider) Stream(ctx context.Context, req *llm.Request) (llm.StreamResponse, error) {\n    // 1. Translate llm.Request to your provider's specific API format\n    apiReq := translate(req)\n\n    // 2. Call your backend's streaming API\n    resp, _ := p.client.CallStreaming(ctx, apiReq)\n\n    // 3. Return an iterator\n    return func(yield func(message.AssistantChunk, error) bool) {\n        for {\n            chunk, err := resp.Next()\n            if err != nil {\n                yield(message.AssistantChunk{}, err)\n                return\n            }\n\n            // 4. Translate back to Loom's AssistantChunk\n            loomChunk := translateBack(chunk)\n            if !yield(loomChunk, nil) {\n                return\n            }\n        }\n    }, nil\n}\n</code></pre>"},{"location":"guides/custom-providers/#3-mapping-messages-tools","title":"3. Mapping Messages &amp; Tools","text":"<p>Most of the work in creating a provider involves mapping between Loom's block-based message system and the provider's specific message format (e.g., Anthropic's <code>ContentBlock</code> vs OpenAI's <code>ToolCall</code>).</p>"},{"location":"guides/custom-providers/#key-mapping-areas","title":"Key Mapping Areas:","text":"<ul> <li>Multimodal Content: Mapping <code>message.ImageBlock</code> to the provider's base64 or URL format.</li> <li>Tool Definitions: Translating <code>tool.Definition</code> into the provider's JSON schema format.</li> <li>Tool Calls: Capturing tool call IDs and arguments from the stream.</li> </ul>"},{"location":"guides/custom-providers/#4-registering-your-provider","title":"4. Registering Your Provider","text":"<p>Once implemented, you can register your provider with the <code>llm.Registry</code> to make it available to the rest of your application.</p> <pre><code>registry := llm.NewRegistry()\nregistry.Register(\"my-provider\", func() (llm.Provider, error) {\n    return &amp;MyCustomProvider{}, nil\n})\n</code></pre>"},{"location":"guides/custom-providers/#summary","title":"Summary","text":"<ul> <li>Implementing a custom provider allows Loom to talk to any internal or specialized LLM API.</li> <li>Focus on accurate mapping of <code>llm.Request</code> and <code>message.AssistantChunk</code>.</li> <li>Use the <code>llm/openai</code> or <code>llm/anthropic</code> packages as reference implementations.</li> </ul>"},{"location":"guides/hitl/","title":"Human-in-the-Loop (HITL) \ud83e\udd1d","text":"<p>Human-in-the-loop patterns allow you to pause an AI workflow and wait for human approval or additional input. This is critical for high-stakes tasks where an LLM should not act autonomously.</p>"},{"location":"guides/hitl/#1-the-interrupt-pattern","title":"1. The Interrupt Pattern","text":"<p>The most basic HITL pattern involves using <code>graph.Interrupt()</code> to stop execution at a specific node.</p> <pre><code>builder.AddNode(\"Review\", func(ctx context.Context, s MyState) (graph.Command[MyState], error) {\n    if s.RequiresApproval &amp;&amp; !s.IsApproved {\n        // Stop execution and save checkpoint\n        return graph.Interrupt(), nil \n    }\n    return graph.Update(func(s MyState) MyState { return s }), nil\n})\n</code></pre>"},{"location":"guides/hitl/#2-waiting-for-input","title":"2. Waiting for Input","text":"<p>When a graph is interrupted, it returns a <code>Snapshot</code>. You can then present the current state to a user (e.g., in a web UI) and wait for them to provide feedback.</p> <pre><code>// 1. Initial execution stops at the Review node\nsnapshot, _ := g.Execute(ctx, initialInput, nil)\n\n// ... UI waits for human button click ...\n\n// 2. Resume with the human's input\nhumanInput := graph.Update(func(s MyState) MyState {\n    s.IsApproved = true\n    return s\n})\n\n// Resume from the previous location\nnextSnapshot, _ := g.Execute(ctx, humanInput, &amp;snapshot.Location)\n</code></pre>"},{"location":"guides/hitl/#3-state-forking-time-travel","title":"3. State Forking (Time Travel)","text":"<p>Because Loom uses checkpoints, you can \"go back in time\" by resuming from an older checkpoint with different inputs. This is useful for testing different paths or correcting an agent's mistakes.</p> <pre><code>// Resume from a specific historical checkpoint ID\nforkLocation := &amp;graph.Location{\n    ThreadID: \"session-1\",\n    CheckpointID: \"cp-5\", // ID from a previous snapshot\n}\n\nsnapshot, _ := g.Execute(ctx, correctionCommand, forkLocation)\n</code></pre>"},{"location":"guides/hitl/#4-breakpoints","title":"4. Breakpoints","text":"<p>Instead of hardcoding interrupts into your nodes, you can set dynamic breakpoints on the graph builder. (Coming soon in Roadmap).</p>"},{"location":"guides/hitl/#summary","title":"Summary","text":"<ul> <li>Interrupts: Pause the graph and save state.</li> <li>Resumption: Provide new input and resume from a saved <code>Location</code>.</li> <li>State Manipulation: Use <code>graph.Update</code> during resumption to inject human decisions into the state.</li> </ul>"},{"location":"guides/llm-package/","title":"LLM Package \ud83e\udde0","text":"<p>The <code>llm</code> package provides a high-level abstraction over different Large Language Model (LLM) backends. It allows you to interact with models from various providers (OpenAI, Anthropic, Google, Ollama) using a consistent API.</p>"},{"location":"guides/llm-package/#core-components","title":"Core Components","text":"<ul> <li><code>Model</code>: The primary interface for interacting with an LLM. It supports both blocking (<code>Invoke</code>) and streaming (<code>Stream</code>) calls.</li> <li><code>Provider</code>: The backend-specific implementation that translates generic requests into provider-specific API calls.</li> <li><code>Registry</code>: A central store for managing and instantiating providers without creating hard dependencies.</li> </ul>"},{"location":"guides/llm-package/#1-using-the-model-api","title":"1. Using the Model API","text":"<p>The <code>llm.Model</code> provides a fluent API for configuring and calling LLMs.</p> <pre><code>import (\n    \"github.com/masterkeysrd/loom/llm\"\n    \"github.com/masterkeysrd/loom/llm/openai\"\n)\n\n// 1. Create a provider\nprovider, _ := loomopenai.NewDefaultProvider()\n\n// 2. Instantiate a model\nmodel := llm.NewModel(provider, \"gpt-4o\")\n\n// 3. Configure parameters (Fluent API)\nmodel = model.\n    WithTemperature(0.7).\n    WithMaxTokens(1000).\n    WithStop(\"User:\", \"Assistant:\")\n\n// 4. Invoke the model (blocking)\nresp, err := model.Invoke(ctx, messages)\n\n// 5. Stream the model (real-time)\n// Chunks are automatically forwarded to any stream.Writer in the context.\nstream, err := model.Stream(ctx, messages)\nfor chunk, err := range stream {\n    fmt.Print(chunk.Content.Text())\n}\n</code></pre>"},{"location":"guides/llm-package/#2-advanced-configuration","title":"2. Advanced Configuration","text":""},{"location":"guides/llm-package/#structured-output-json-schema","title":"Structured Output (JSON Schema)","text":"<p>If a provider supports it, you can enforce a specific JSON schema for the model's response. <pre><code>model = model.WithStructuredOutput(myJsonSchema)\n</code></pre></p>"},{"location":"guides/llm-package/#thinking-reasoning-mode","title":"Thinking (Reasoning) Mode","text":"<p>For models that support \"thinking\" or extended reasoning (like Anthropic Claude or Google Gemini), you can configure the reasoning budget. <pre><code>model = model.WithThinking(4000) // 4000 tokens for thinking\n</code></pre></p>"},{"location":"guides/llm-package/#extensions-hints","title":"Extensions &amp; Hints","text":"<p>Providers often have specialized features (like caching) that aren't part of the common LLM parameters. Loom uses a type-safe Extensions system to pass these hints.</p> <p>Extensions come in two flavors: 1.  Call Extensions (<code>llm.Extension</code>): Apply to the entire request (e.g., enable system prompt caching). 2.  Message Extensions (<code>message.Extension</code>): Apply to a specific turn in the conversation (e.g., mark a checkpoint).</p> <pre><code>// Example: Setting a call-level extension for Anthropic\nmodel = model.WithExtension(loomanthropic.PromptCaching{CacheHeader: true})\n\n// Example: Passing a call-level extension for a single call\nmodel.Invoke(ctx, msgs, llm.WithExtensionOption(loomopenai.PromptCache{\n    Key: \"user-session-123\",\n}))\n</code></pre>"},{"location":"guides/llm-package/#3-middleware-hooks","title":"3. Middleware &amp; Hooks","text":"<p>Middleware allows you to wrap LLM calls to inject cross-cutting concerns like logging, auditing, or automatic metadata injection.</p> <pre><code>model = model.WithMiddleware(func(next llm.Streamer) llm.Streamer {\n    return func(ctx context.Context, req *llm.Request) (llm.StreamResponse, error) {\n        fmt.Printf(\"Calling model: %s\\n\", req.Model)\n\n        // You can also modify the request before it reaches the provider\n        if len(req.Messages) &gt; 10 {\n            req.Extensions[myprovider.CustomExt{}.ExtensionID()] = myprovider.CustomExt{Enabled: true}\n        }\n\n        return next(ctx, req)\n    }\n})\n</code></pre>"},{"location":"guides/llm-package/#4-cache-management","title":"4. Cache Management","text":"<p>For providers that support explicit resource management (like Google Gemini's Context Caching), Loom provides a <code>CacheManager</code> API.</p> <pre><code>// 1. Create a long-lived cache from a list of messages\ncacheID, err := model.\n    WithExtension(loomgenai.CacheCreation{DisplayName: \"Project Docs\"}).\n    CreateCache(ctx, documentMessages)\n\n// 2. Use the cache ID in subsequent calls\nresp, _ := model.Invoke(ctx, prompt, llm.WithExtensionOption(loomgenai.ContextCache{ID: cacheID}))\n\n// 3. Delete when no longer needed\nmodel.DeleteCache(ctx, cacheID)\n</code></pre>"},{"location":"guides/llm-package/#5-using-the-llm-registry","title":"5. Using the LLM Registry","text":"<p>The <code>Registry</code> is useful for decoupling your application from specific provider packages. It allows you to register provider factories and retrieve them by name.</p>"},{"location":"guides/llm-package/#summary","title":"Summary","text":"<ul> <li>Use <code>llm.NewModel</code> to start interacting with an LLM.</li> <li>Use the <code>With*</code> methods to configure model parameters.</li> <li>Use <code>Invoke</code> for simple request-response and <code>Stream</code> for real-time applications.</li> <li>Use <code>llm.Registry</code> to manage multiple providers in a large application.</li> </ul>"},{"location":"guides/mcp/","title":"Model Context Protocol (MCP) \ud83d\udd0c","text":"<p>The Model Context Protocol (MCP) is an open standard that enables developers to build secure, two-way connections between their data sources and AI-powered tools. Loom provides first-class support for MCP, allowing you to easily integrate tools, resources, and prompts from any MCP-compliant server.</p>"},{"location":"guides/mcp/#1-core-concepts","title":"1. Core Concepts","text":"<p>Loom's MCP package revolves around three main components:</p> <ul> <li><code>Client</code>: A handle to a single MCP server.</li> <li><code>MultiClient</code>: A registry for managing multiple MCP servers simultaneously.</li> <li><code>SessionClient</code>: A stateful, session-scoped client for executing tools and fetching resources.</li> </ul>"},{"location":"guides/mcp/#2-configuration","title":"2. Configuration","text":"<p>MCP servers can be local subprocesses (stdio) or remote servers (HTTP/SSE).</p> <pre><code>config := mcp.Config{\n    Transport: \"stdio\",\n    Command:   \"python\",\n    Args:      []string{\"my_server.py\"},\n    Env:       map[string]string{\"API_KEY\": \"secret\"},\n}\n\n// Or for a remote server\nconfig := mcp.Config{\n    Transport: \"http\",\n    URL:       \"https://mcp.example.com/sse\",\n    Headers:   map[string]string{\"Authorization\": \"Bearer token\"},\n}\n</code></pre>"},{"location":"guides/mcp/#3-integrating-tools","title":"3. Integrating Tools","text":"<p>One of the most powerful features of MCP is the ability to dynamically extract tools from a server and use them within Loom.</p> <pre><code>client := mcp.NewClient(config)\nctx := context.Background()\n\n// Start a session\nsession, _ := client.Session(ctx)\ndefer session.Close()\n\n// Extract tools as native Loom tools\ntools, _ := session.Tools(ctx)\n\n// Add them to a Loom tool container\ncontainer := tool.NewContainer(tools...)\n</code></pre>"},{"location":"guides/mcp/#4-resources-and-prompts","title":"4. Resources and Prompts","text":"<p>MCP servers can also expose data resources and pre-defined prompt templates.</p>"},{"location":"guides/mcp/#fetching-resources","title":"Fetching Resources","text":"<p>Resources are fetched as <code>ResourceContents</code>, which can contain text or binary data.</p> <pre><code>// Fetch all resources from a server\nresources, _ := client.GetResources(ctx, nil)\n\nfor _, res := range resources {\n    fmt.Printf(\"URI: %s, Text: %s\\n\", res.URI, res.Text)\n}\n</code></pre>"},{"location":"guides/mcp/#using-prompts","title":"Using Prompts","text":"<p>MCP prompts are returned as a slice of native Loom messages (<code>[]message.Message</code>), making them ready to be injected into a conversation.</p> <pre><code>// Fetch a prompt template with arguments\nmessages, _ := client.GetPrompt(ctx, \"summarize\", map[string]string{\n    \"text\": \"The long text to summarize...\",\n})\n\n// Add them to your message history\nhistory = append(history, messages...)\n</code></pre>"},{"location":"guides/mcp/#5-multi-server-management","title":"5. Multi-Server Management","text":"<p>The <code>MultiClient</code> allows you to manage multiple MCP servers under a single registry.</p> <pre><code>configs := map[string]mcp.Config{\n    \"weather\": {Transport: \"http\", URL: \"http://localhost:8000/mcp\"},\n    \"db\":      {Transport: \"stdio\", Command: \"mcp-db-server\"},\n}\n\nmc := mcp.NewMultiClient(configs)\n\n// Fetch tools from the weather server\nsession, _ := mc.Session(ctx, \"weather\")\nweatherTools, _ := session.Tools(ctx)\n</code></pre>"},{"location":"guides/mcp/#6-authentication","title":"6. Authentication","text":"<p>Loom supports both static and dynamic authentication flows for MCP.</p>"},{"location":"guides/mcp/#static-auth","title":"Static Auth","text":"<p>Use <code>Headers</code> in <code>Config</code> for simple API keys or <code>Env</code> for local subprocess credentials.</p>"},{"location":"guides/mcp/#dynamic-auth-oauth2-enterprise","title":"Dynamic Auth (OAuth2 &amp; Enterprise)","text":"<p>For servers requiring interactive login, use the <code>AuthProvider</code> interface.</p> <p><pre><code>config := mcp.Config{\n    Transport: \"http\",\n    URL:       \"https://enterprise.mcp.com/sse\",\n    Auth: &amp;mcp.EnterpriseProvider{\n        ClientID:     \"my-id\",\n        IdPIssuerURL: \"https://okta.example.com\",\n        // ...\n    },\n}\n</code></pre> When a session is started, Loom will automatically spin up a local callback server and prompt the user to authenticate in their browser.</p>"},{"location":"guides/mcp/#7-elicitation-interactive-input","title":"7. Elicitation (Interactive Input)","text":"<p>Elicitation allows an MCP server to request information or actions from the user during a session (e.g., asking for a password or confirming an action).</p> <p>To support this, implement the <code>ElicitationProvider</code> interface and provide it in your <code>Config</code>.</p> <pre><code>type MyElicitProvider struct{}\n\nfunc (p *MyElicitProvider) HandleElicit(ctx context.Context, params *mcp.ElicitParams) (*mcp.ElicitResult, error) {\n    fmt.Println(\"Server request:\", params.Message)\n    // Handle form input or URL action here...\n    return &amp;mcp.ElicitResult{Action: \"accept\"}, nil\n}\n\nfunc (p *MyElicitProvider) HandleElicitComplete(ctx context.Context, params *mcp.ElicitationCompleteParams) {\n    fmt.Println(\"Elicitation complete:\", params.ElicitationID)\n}\n\nconfig := mcp.Config{\n    Transport:   \"stdio\",\n    Command:     \"my-server\",\n    Elicitation: &amp;MyElicitProvider{},\n}\n</code></pre> <p>Loom provides the infrastructure to bridge these server-side requests to your custom UI logic.</p>"},{"location":"guides/mcp/#summary","title":"Summary","text":"<ul> <li>Standardized: Connect to any MCP-compliant data source.</li> <li>Native Integration: MCP tools and prompts map directly to Loom types.</li> <li>Secure: Built-in support for OAuth2 and Enterprise OIDC.</li> <li>Flexible: Manage multiple servers via <code>MultiClient</code>.</li> </ul>"},{"location":"guides/memory/","title":"Memory &amp; Context Management \ud83e\udde0","text":"<p>As conversations with AI agents grow, they can quickly exceed the LLM's context window (token limit). Loom provides automated tools to manage this history through Trimming and Summarization.</p>"},{"location":"guides/memory/#1-trimming-messages","title":"1. Trimming Messages","text":"<p>Trimming is a simple way to keep only the most recent messages. Loom provides a flexible <code>Trimmer</code> that can handle different strategies.</p> <pre><code>trimmed, _ := message.TrimMessages(ctx, history, 4000, &amp;message.TrimConfig{\n    Strategy:      message.TrimStrategyLast, // Keep the N most recent tokens\n    IncludeSystem: true,                     // Always keep the system instruction\n})\n</code></pre>"},{"location":"guides/memory/#trimming-strategies","title":"Trimming Strategies","text":"<ul> <li><code>TrimStrategyLast</code>: Keeps the most recent messages that fit within the token limit.</li> <li><code>TrimStrategyFirst</code>: Keeps the oldest messages (rarely used for history).</li> </ul>"},{"location":"guides/memory/#2-automatic-summarization","title":"2. Automatic Summarization","text":"<p>Summarization is a more advanced technique where the agent \"remembers\" the gist of the conversation by condensing old messages into a summary.</p>"},{"location":"guides/memory/#setting-up-a-summarizer","title":"Setting up a Summarizer","text":"<pre><code>import \"github.com/masterkeysrd/loom/memory\"\n\nsummarizer, _ := memory.NewSummarizer(model, memory.SummarizerConfig{\n    TokenCounter: modelProvider.(llm.TokenCounter),\n    Triggers: []memory.SummarizerTrigger{\n        // Summarize when the conversation exceeds 4000 tokens\n        memory.TriggerSummaryOnTokenCount(4000),\n    },\n})\n</code></pre>"},{"location":"guides/memory/#using-the-summarizer-in-a-node","title":"Using the Summarizer in a Node","text":"<pre><code>func llmNode(ctx context.Context, s MyState) (graph.Command[AppState], error) {\n    // 1. Summarize if a trigger is met\n    newHistory, _ := summarizer.Summarize(ctx, s.Messages)\n\n    // 2. Invoke the model with the condensed history\n    resp, _ := model.Invoke(ctx, newHistory)\n\n    return graph.Update(func(s MyState) MyState {\n        s.Messages = append(newHistory, resp)\n        return s\n    }), nil\n}\n</code></pre>"},{"location":"guides/memory/#best-practices","title":"Best Practices","text":"<ul> <li>Combine Strategies: Use summarization for long-term \"memory\" and trimming as a safety net to ensure you never exceed hard limits.</li> <li>System Message: Always preserve your system instruction (prompt) during trimming/summarization.</li> <li>Token Accuracy: Use the <code>TokenCounter</code> provided by your LLM provider for the most accurate results.</li> </ul>"},{"location":"guides/observability/","title":"Observability","text":"<p>Loom provides first-class support for observability using OpenTelemetry (OTel). This allows you to trace the execution of your agents, monitor performance, and debug complex multi-step workflows.</p>"},{"location":"guides/observability/#quick-start","title":"Quick Start","text":"<p>The easiest way to get started is to use the built-in <code>telemetry</code> package and Loom Studio.</p> <pre><code>import \"github.com/masterkeysrd/loom/telemetry\"\n\n// Initialize telemetry (usually in main)\nshutdown, _ := telemetry.Init(ctx, telemetry.Config{ServiceName: \"my-agent\"})\ndefer shutdown(ctx)\n\n// Start a span\nctx, span := telemetry.Start(ctx, \"my-operation\")\ndefer span.End()\n\n// Log a custom attribute\nspan.SetAttributes(telemetry.WithLoomThread(\"thread-123\"))\n</code></pre>"},{"location":"guides/observability/#loom-studio","title":"Loom Studio","text":"<p>Loom Studio is a built-in control plane for your agents. It provides a real-time visualization layer on top of your telemetry data.</p>"},{"location":"guides/observability/#running-loom-studio","title":"Running Loom Studio","text":"<p>To start Loom Studio, run:</p> <pre><code>loom studio\n</code></pre> <p>By default, it will: - Open a web dashboard on <code>http://localhost:8080</code>. - Listen for OTLP gRPC telemetry on <code>localhost:4317</code>. - Listen for OTLP HTTP telemetry on <code>localhost:4318</code>. - Store data in a local SQLite database at <code>.loom/telemetry.db</code>.</p>"},{"location":"guides/observability/#core-views","title":"Core Views","text":"<ol> <li>Dashboard: A high-level overview of system health, featuring token consumption, LLM call volume, P50 latency, and tool invocation counts.</li> <li>Trace Explorer: An audit log of every execution thread. You can search by Thread ID or Graph name to find specific interactions.</li> <li>Trace Waterfall: A detailed Gantt-style view of a single execution. It breaks down the timing of every LLM call, node transition, and tool execution.</li> <li>Metrics Explorer: A deep-dive into time-series data. It supports Temporal Bucketing (1s to 1h intervals) to smooth out data and ensure fast rendering even with millions of points.</li> </ol>"},{"location":"guides/observability/#semantic-conventions-v1411","title":"Semantic Conventions (v1.41.1)","text":"<p>Loom strictly adheres to the latest OpenTelemetry GenAI Semantic Conventions. This means spans and metrics are standardized: - Inference Spans: Named <code>chat {model}</code> with attributes for tokens (input/output/reasoning/cache), temperature, and response IDs. - Tool Spans: Named <code>execute_tool {name}</code>. - Histograms: Use the official explicit bucket boundaries for precise distribution analysis.</p>"},{"location":"guides/observability/#automatic-tracing","title":"Automatic Tracing","text":"<p>Loom's <code>Model</code> and <code>Graph</code> packages automatically emit OpenTelemetry spans and metrics for: - LLM requests (using GenAI semantic conventions). - Node entry and exit. - Graph execution durations and node invocations.</p>"},{"location":"guides/observability/#capturing-sensitive-content","title":"Capturing Sensitive Content","text":"<p>By default, Loom does not record sensitive content (prompts, completion text, tool results) to protect your privacy. You can opt-in to content recording using <code>telemetry.WithContentRecording</code>:</p> <pre><code>ctx = telemetry.WithContentRecording(ctx)\n// Subsequent LLM and tool calls in this context will record their payloads\n</code></pre>"},{"location":"guides/observability/#production-observability","title":"Production Observability","text":"<p>Since Loom uses standard OpenTelemetry under the hood, you can easily point it to any OTel-compliant backend (Datadog, Honeycomb, Jaeger, etc.) by setting the standard environment variables:</p> <pre><code>export OTEL_EXPORTER_OTLP_ENDPOINT=\"https://api.honeycomb.io\"\nexport OTEL_EXPORTER_OTLP_HEADERS=\"x-honeycomb-team=your-api-key\"\n</code></pre> <p>If your application already has OpenTelemetry configured, Loom will automatically use your existing <code>TracerProvider</code> and <code>MeterProvider</code> without any additional setup.</p>"},{"location":"guides/observability/#best-practices","title":"Best Practices","text":"<ul> <li>Trace IDs: Always propagate <code>context.Context</code> through your application to ensure spans are correctly parented.</li> <li>Attributes: Use standard semantic conventions for custom attributes when possible to ensure compatibility with various observability tools.</li> <li>Sampling: In high-throughput production environments, consider configuring a sampler to reduce the volume of telemetry data.</li> </ul>"},{"location":"guides/persistence/","title":"Persistence &amp; State \ud83d\udcbe","text":"<p>One of Loom's core strengths is its \"state-first\" architecture. Every time a node executes, Loom saves a checkpoint of your workflow's state. This allows you to resume execution after a failure, restart a long-running process, or even pause a graph to wait for human input.</p>"},{"location":"guides/persistence/#how-checkpointing-works","title":"How Checkpointing Works","text":"<p>Loom uses a <code>Checkpointer</code> interface to persist snapshots of the <code>State</code>. A snapshot includes: - The current values in your state struct. - The execution history (which nodes have run). - The <code>Location</code> (Thread ID and Checkpoint ID) for resumption.</p>"},{"location":"guides/persistence/#1-setting-up-a-checkpointer","title":"1. Setting up a Checkpointer","text":"<p>Loom provides built-in support for SQLite and PostgreSQL.</p>"},{"location":"guides/persistence/#sqlite","title":"SQLite","text":"<pre><code>import (\n    \"database/sql\"\n    \"github.com/masterkeysrd/loom/checkpoint/sqlite\"\n    _ \"github.com/mattn/go-sqlite3\"\n)\n\ndb, _ := sql.Open(\"sqlite3\", \"loom.db\")\ncp, _ := sqlite.NewCheckpointer(db)\n\n// Attach to the graph builder\nbuilder.WithCheckpointer(cp)\n</code></pre>"},{"location":"guides/persistence/#postgresql","title":"PostgreSQL","text":"<pre><code>import (\n    \"github.com/masterkeysrd/loom/checkpoint/pg\"\n)\n\ncp, _ := pg.NewCheckpointer(\"postgres://user:pass@localhost/dbname\")\nbuilder.WithCheckpointer(cp)\n</code></pre>"},{"location":"guides/persistence/#2-human-in-the-loop-interrupts","title":"2. Human-in-the-Loop (Interrupts)","text":"<p>You can pause a graph's execution using the <code>graph.Interrupt()</code> command. This is essential for workflows that require human approval or external input.</p> <pre><code>builder.AddNode(\"Approval\", func(ctx context.Context, s MyState) (graph.Command[MyState], error) {\n    if !s.IsApproved {\n        return graph.Interrupt(), nil // Execution stops here and state is saved\n    }\n    return graph.Update(func(s MyState) MyState { return s }), nil\n})\n</code></pre>"},{"location":"guides/persistence/#3-resuming-a-thread","title":"3. Resuming a Thread","text":"<p>When a graph is interrupted or finishes, it returns a <code>Snapshot</code>. You can use the <code>snapshot.Location</code> to resume execution later.</p> <pre><code>// Initial execution\nsnapshot, _ := g.Execute(ctx, initialCmd, nil)\nthreadID := snapshot.Location.ThreadID\n\n// ... Later, when you want to resume ...\n\n// Resume from the last checkpoint using the ThreadID\nresumeLocation := &amp;graph.Location{ThreadID: threadID}\nnextSnapshot, _ := g.Execute(ctx, nil, resumeLocation)\n</code></pre>"},{"location":"guides/persistence/#best-practices","title":"Best Practices","text":"<ul> <li>State Serialization: Ensure all fields in your state struct are JSON-serializable if you are using standard checkpointers.</li> <li>Thread Management: Use unique <code>ThreadID</code>s to separate different user sessions or independent tasks.</li> <li>Idempotency: Since a node might be retried if a checkpoint fails, try to make your node functions idempotent where possible.</li> </ul>"},{"location":"guides/persistent-store/","title":"Persistent Store \ud83d\udcbe","text":"<p>The <code>store</code> package provides a namespaced key-value store for persistent, searchable long-term memory. It gives AI agents the ability to read and write structured data that survives across graph executions, process restarts, and conversations.</p>"},{"location":"guides/persistent-store/#overview","title":"Overview","text":"<p>The store follows a clean interface pattern: a core <code>Store</code> interface in the parent package, with backend implementations in sub-packages. All values are JSON-serialized internally \u2014 callers pass Go pointers and the store marshals/unmarshals automatically.</p> <pre><code>store/\n\u251c\u2500\u2500 store.go        # Core Store interface\n\u251c\u2500\u2500 options.go      # Variadic options (PutOption, SearchOption, etc.)\n\u251c\u2500\u2500 context.go      # WithStore / FromContext integration\n\u251c\u2500\u2500 generic.go      # Generic helpers: Get[T], Search[T]\n\u251c\u2500\u2500 memorydb/       # In-memory backend (dev / testing)\n\u2514\u2500\u2500 sqlite/         # SQLite backend (production)\n</code></pre>"},{"location":"guides/persistent-store/#core-interface","title":"Core Interface","text":"<p>The <code>Store</code> interface exposes four operations over a <code>namespace/key</code> address space:</p> Method Purpose <code>Put</code> Marshal <code>value</code> to JSON and store it under <code>namespace/key</code>. Upserts if the key exists. <code>Get</code> Retrieve a single item by <code>namespace/key</code> and unmarshal into a pointer. Returns <code>store.ErrNotFound</code> if missing. <code>Search</code> Find all items within a namespace. Supports filtering by prefix, limit, and offset. <code>Delete</code> Remove an item. Idempotent \u2014 returns <code>nil</code> if the key does not exist."},{"location":"guides/persistent-store/#gorm-style-api","title":"GORM-Style API","text":"<p>The API follows the GORM / <code>json.Unmarshal</code> pattern: callers pass a pointer and the store populates it. No <code>json.RawMessage</code> leaks into the public API.</p> <pre><code>s := store.FromContext(ctx)\n\nvar prefs UserPrefs\nif err := s.Get(ctx, \"user:123\", \"preferences\", &amp;prefs); err != nil { ... }\n</code></pre>"},{"location":"guides/persistent-store/#generic-package-level-helpers","title":"Generic Package-Level Helpers","text":"<p>For callers who prefer typed returns over pointer passing:</p> <pre><code>memories, err := store.Get[Memories](ctx, s, \"user:123\", \"memories\")\n\nitems, err := store.Search[Item](ctx, s, \"user:123\", store.WithLimit(10))\n</code></pre>"},{"location":"guides/persistent-store/#setting-up-the-store","title":"Setting Up the Store","text":""},{"location":"guides/persistent-store/#in-memory-backend-development","title":"In-Memory Backend (Development)","text":"<p>The <code>memorydb</code> backend is a zero-dependency, concurrent-safe in-memory store. Ideal for development, testing, and unit tests.</p> <pre><code>import \"github.com/masterkeysrd/loom/store/memorydb\"\n\ns := memorydb.New()\nbuilder.WithStore(s)\n</code></pre>"},{"location":"guides/persistent-store/#sqlite-backend-production","title":"SQLite Backend (Production)","text":"<p>The <code>sqlite</code> backend persists namespaced key-value data to a <code>store_items</code> table, enabling durable long-term memory across process restarts.</p> <pre><code>import (\n    \"database/sql\"\n    \"github.com/masterkeysrd/loom/store/sqlite\"\n    _ \"modernc.org/sqlite\"\n)\n\ndb, err := sql.Open(\"sqlite\", \"loom.db\")\nif err != nil { ... }\n\ns, err := sqlite.NewStore(db)\nif err != nil { ... }\n\nbuilder.WithStore(s)\n</code></pre> <p>The schema is managed automatically via a sequential migration runner:</p> <pre><code>CREATE TABLE IF NOT EXISTS store_migrations (\n    version    INTEGER PRIMARY KEY,\n    applied_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now'))\n);\n\nPRAGMA journal_mode=WAL;\n\nCREATE TABLE IF NOT EXISTS store_items (\n    namespace  TEXT NOT NULL,\n    key        TEXT NOT NULL,\n    value      BLOB NOT NULL,\n    created_at TEXT NOT NULL,\n    updated_at TEXT NOT NULL,\n    PRIMARY KEY (namespace, key)\n);\n\nCREATE INDEX IF NOT EXISTS idx_store_items_namespace ON store_items (namespace);\n</code></pre> <p>Call <code>sqlite.Migrate(db)</code> manually if you need to run migrations separately from <code>NewStore</code>. It is safe to call multiple times (idempotent).</p>"},{"location":"guides/persistent-store/#usage-patterns","title":"Usage Patterns","text":""},{"location":"guides/persistent-store/#retrieving-the-store","title":"Retrieving the Store","text":"<p>The store is injected into the context. Retrieve it with <code>store.FromContext</code>:</p> <pre><code>func myNode(ctx context.Context, s MyState) (graph.Command[MyState], error) {\n    store := store.FromContext(ctx)\n    if store == nil {\n        return nil, fmt.Errorf(\"store not configured\")\n    }\n    // ...\n}\n</code></pre>"},{"location":"guides/persistent-store/#per-user-scoping","title":"Per-User Scoping","text":"<p>Use namespaced keys to isolate data per user, session, or agent:</p> <pre><code>s := store.FromContext(ctx)\n\n// Per-user preferences\nns := fmt.Sprintf(\"%s:user-data\", userID)\nvar prefs UserPrefs\nif err := s.Get(ctx, ns, \"preferences\", &amp;prefs); err != nil { ... }\n\n// Per-agent knowledge\nns = fmt.Sprintf(\"%s:knowledge\", agentID)\ns.Put(ctx, ns, \"latest-finding\", result)\n</code></pre>"},{"location":"guides/persistent-store/#inside-a-graph-node","title":"Inside a Graph Node","text":"<pre><code>func myNode(ctx context.Context, s MyState) (graph.Command[MyState], error) {\n    store := store.FromContext(ctx)\n\n    // GORM-style\n    var prefs UserPrefs\n    err := s.Get(ctx, fmt.Sprintf(\"%s:user-data\", s.UserID), \"preferences\", &amp;prefs)\n\n    // Generic-style\n    memories, err := store.Get[Memories](ctx, store, fmt.Sprintf(\"%s:user-data\", s.UserID), \"memories\")\n\n    return graph.Update(func(s MyState) MyState { return s }), nil\n}\n</code></pre>"},{"location":"guides/persistent-store/#inside-a-tool","title":"Inside a Tool","text":"<pre><code>func myTool(ctx context.Context, args MyArgs) (string, error) {\n    s := store.FromContext(ctx)\n    if err := s.Put(ctx, \"agent:knowledge\", \"latest-finding\", result); err != nil {\n        return \"\", err\n    }\n    return \"saved\", nil\n}\n</code></pre>"},{"location":"guides/persistent-store/#search-options","title":"Search Options","text":"<p>The <code>Search</code> method supports variadic options for filtering and pagination:</p> Option Purpose <code>WithKeyPrefix(prefix)</code> Filter results to keys matching the given prefix <code>WithLimit(n)</code> Cap the number of items returned <code>WithOffset(n)</code> Skip the first N items (pagination) <pre><code>var items []Item\nerr := s.Search(ctx, \"user:123\", &amp;items,\n    store.WithKeyPrefix(\"mem:\"),\n    store.WithLimit(10),\n    store.WithOffset(20),\n)\n</code></pre>"},{"location":"guides/persistent-store/#best-practices","title":"Best Practices","text":"<ul> <li>Namespace Convention: Use hierarchical namespace strings for logical grouping, e.g., <code>\"user:123:data\"</code> or <code>\"agent:knowledge\"</code>.</li> <li>Thread Safety: Both backends are safe for concurrent use \u2014 <code>memorydb</code> uses <code>sync.RWMutex</code>, <code>sqlite</code> relies on <code>sql.DB</code> internal locking.</li> <li>Idempotency: <code>Delete</code> is idempotent (returns <code>nil</code> for missing keys). Design node functions to be idempotent where possible, since retries may occur.</li> <li>JSON Serialization: All values are JSON-serialized. Ensure struct fields are JSON-serializable.</li> <li>Production Choice: Use <code>sqlite</code> for production deployments where persistence across restarts is required. Use <code>memorydb</code> for development and tests.</li> </ul>"},{"location":"guides/quickstart/","title":"Quick Start Guide \ud83e\uddf5","text":"<p>Welcome to Loom! This guide will help you build your first graph-based AI agent. Loom allows you to define complex AI workflows as directed graphs, making it easy to manage state, branching logic, and persistence.</p>"},{"location":"guides/quickstart/#core-concepts","title":"Core Concepts","text":"<p>Before we dive into the code, let's look at the three main building blocks of Loom:</p> <ol> <li>State: A Go struct that holds all the data for your workflow.</li> <li>Nodes: Functions that perform work and update the state.</li> <li>Edges: The connections between nodes that define the flow of execution.</li> </ol>"},{"location":"guides/quickstart/#1-define-your-state","title":"1. Define Your State","text":"<p>Every Loom graph operates on a shared state. This state must implement a <code>Copy()</code> method to ensure that each step in the graph works on an independent snapshot.</p> <pre><code>type AppState struct {\n    Input    string `json:\"input\"`\n    Category string `json:\"category\"`\n    Output   string `json:\"output\"`\n}\n\nfunc (s AppState) Copy() AppState {\n    return s\n}\n</code></pre>"},{"location":"guides/quickstart/#2-initialize-the-graph-builder","title":"2. Initialize the Graph Builder","text":"<p>The <code>graph.Builder</code> is used to assemble your nodes and edges.</p> <pre><code>builder := graph.New[AppState]()\n</code></pre>"},{"location":"guides/quickstart/#3-add-nodes","title":"3. Add Nodes","text":"<p>Nodes are where the logic happens. A node takes the current state and returns a <code>Command</code>, which tells the Loom engine how to update the state or where to go next.</p> <pre><code>builder.AddNode(\"Categorize\", func(ctx context.Context, state AppState) (graph.Command[AppState], error) {\n    category := \"general\"\n    if strings.Contains(strings.ToLower(state.Input), \"math\") {\n        category = \"math\"\n    }\n\n    // Return an Update command to modify the state\n    return graph.Update[AppState](func(s AppState) AppState {\n        s.Category = category\n        return s\n    }), nil\n})\n</code></pre>"},{"location":"guides/quickstart/#4-define-the-workflow-edges","title":"4. Define the Workflow (Edges)","text":"<p>Edges connect your nodes. Loom supports direct edges and Route Edges for conditional branching.</p> <pre><code>// Start at the Categorize node\nbuilder.AddEdge(graph.START, \"Categorize\")\n\n// Branch based on the 'Category' in the state\nbuilder.AddRouteEdge(\"Categorize\", func(s AppState) (string, error) {\n    return s.Category, nil\n}, map[string]string{\n    \"math\":    \"MathLLM\",\n    \"general\": \"GeneralLLM\",\n})\n\n// Both branches lead to the END node\nbuilder.AddEdge(\"MathLLM\", graph.END)\nbuilder.AddEdge(\"GeneralLLM\", graph.END)\n</code></pre>"},{"location":"guides/quickstart/#5-build-and-execute","title":"5. Build and Execute","text":"<p>Once your graph is defined, you can build it and execute it with an initial input.</p> <pre><code>g, _ := builder.Build()\n\n// Execute the graph\ninitialState := AppState{Input: \"What is 2+2?\"}\nsnapshot, _ := g.Execute(ctx, graph.Update(func(s AppState) AppState {\n    return initialState\n}), nil)\n\nfmt.Println(\"Final Output:\", snapshot.State.Output)\n</code></pre>"},{"location":"guides/quickstart/#next-steps","title":"Next Steps","text":"<p>Now that you've built your first graph, check out these advanced guides:</p> <ul> <li>Conversations: Deep dive into message roles, multimodal content, and history.</li> <li>LLM Package: Deep dive into the Model API and Registry.</li> <li>LLM Providers: Learn about OpenAI, Anthropic, Gemini, and Ollama.</li> <li>Persistence &amp; State: Learn how to save and resume your workflows.</li> <li>Human-in-the-Loop: Patterns for human approval and input.</li> <li>Tools &amp; Streaming: Integrate your agents with external tools and real-time updates.</li> <li>Memory Management: Handle long conversations with automated trimming and summarization.</li> <li>Observability: Visualize your graphs and trace execution.</li> <li>Custom Providers: (Advanced) How to add support for new LLM backends.</li> </ul>"},{"location":"guides/tools/","title":"Tools &amp; Streaming \ud83d\udee0\ufe0f","text":"<p>Tools allow your AI agents to interact with external systems, APIs, and databases. Loom provides a robust, type-safe way to define tools and execute them within your graphs.</p>"},{"location":"guides/tools/#1-defining-a-tool","title":"1. Defining a Tool","text":"<p>Loom uses Go reflection to automatically generate the JSON schema for your tools, which is then sent to the LLM.</p> <pre><code>type SearchInput struct {\n    Query string `json:\"query\"`\n}\n\ntype SearchOutput struct {\n    Results []string `json:\"results\"`\n}\n\nsearchTool, _ := tool.New(\n    \"web_search\",          // Name\n    \"Web Search\",         // Display Name\n    \"Searches the web\",   // Description\n    func(ctx context.Context, in SearchInput) (SearchOutput, error) {\n        // Implementation logic\n        return SearchOutput{Results: []string{\"Result 1\"}}, nil\n    },\n)\n</code></pre>"},{"location":"guides/tools/#2-using-the-tool-container","title":"2. Using the Tool Container","text":"<p>The <code>tool.Container</code> groups multiple tools together and handles their execution.</p> <pre><code>container := tool.NewContainer(searchTool, calculatorTool)\n\n// Bind tool definitions to the model\nmodel = model.BindToolDefs(container.Definitions()...)\n</code></pre> <p>In a graph node, you can execute a tool call received from the LLM:</p> <pre><code>resp, err := container.Call(ctx, assistantToolCall)\n</code></pre>"},{"location":"guides/tools/#3-streaming-tools","title":"3. Streaming Tools","text":"<p>Loom supports \"Streaming Tools\" which can report progress to the UI or yield multiple parts of a result (e.g., text followed by an image).</p> <pre><code>processorTool, _ := tool.NewStreaming(\n    \"analyze_data\",\n    \"Data Processor\",\n    \"Analyzes data and returns results\",\n    func(ctx context.Context, in struct{ Dataset string }) (tool.ToolStream, error) {\n        return func(yield func(message.ToolChunk, error) bool) {\n            // Report progress\n            curr := 1.0; total := 2.0\n            yield(message.ToolChunk{\n                Progress: \"Analyzing...\",\n                ProgressCurrent: &amp;curr,\n                ProgressTotal: &amp;total,\n            }, nil)\n\n            // Yield result\n            yield(message.ToolChunk{\n                Content: message.Content{&amp;message.TextBlock{Text: \"Result\"}},\n            }, nil)\n        }, nil\n    },\n)\n</code></pre>"},{"location":"guides/tools/#4-mcp-tools","title":"4. MCP Tools","text":"<p>Loom provides first-class support for the Model Context Protocol (MCP). You can dynamically extract tools from any MCP-compliant server and add them to your <code>tool.Container</code>.</p> <pre><code>// Connect to an MCP server\nclient := mcp.NewClient(mcp.Config{\n    Transport: \"stdio\",\n    Command:   \"python\",\n    Args:      []string{\"my_server.py\"},\n})\n\n// Extract tools\nsession, _ := client.Session(ctx)\nmcpTools, _ := session.Tools(ctx)\n\n// Integrate with Loom container\ncontainer := tool.NewContainer()\ncontainer.AddTools(mcpTools...)\n</code></pre> <p>See the MCP Guide for more details.</p>"},{"location":"guides/tools/#5-graph-streaming","title":"5. Graph Streaming","text":"<p>To observe tool progress and token-level updates from the LLM, use the <code>g.Stream</code> method.</p> <pre><code>events, _ := g.Stream(ctx, input, nil)\n\nfor event, err := range events {\n    // The 'Source' field identifies the origin (e.g., \"llm:gpt-4o\" or \"tool:web_search\")\n    fmt.Printf(\"[%s] \", event.Source)\n\n    switch event.Event {\n    case graph.EventToolProgress:\n        chunk := event.Data.(message.ToolChunk)\n        fmt.Println(\"Progress:\", chunk.Progress)\n    case graph.EventLLMChunk:\n        chunk := event.Data.(message.AssistantChunk)\n        fmt.Print(chunk.Content.Text())\n    }\n}\n</code></pre>"},{"location":"guides/tools/#summary","title":"Summary","text":"<ul> <li><code>tool.New</code>: For standard request-response tools.</li> <li><code>tool.NewStreaming</code>: For tools that need to report progress or stream multi-part results.</li> <li><code>tool.Container</code>: For managing and executing multiple tools.</li> <li><code>g.Stream</code>: For real-time event monitoring.</li> </ul>"},{"location":"guides/providers/anthropic/","title":"Anthropic Provider \ud83d\udd0c","text":"<p>The Anthropic provider supports the Claude family of models, including Claude 3.5 Sonnet and Haiku.</p>"},{"location":"guides/providers/anthropic/#1-setup-authentication","title":"1. Setup &amp; Authentication","text":"<p>Set the <code>ANTHROPIC_API_KEY</code> environment variable.</p> <pre><code>export ANTHROPIC_API_KEY='your-api-key-here'\n</code></pre>"},{"location":"guides/providers/anthropic/#2-basic-usage","title":"2. Basic Usage","text":"<pre><code>import (\n    \"github.com/masterkeysrd/loom/llm\"\n    \"github.com/masterkeysrd/loom/llm/anthropic\"\n)\n\nprovider, err := loomanthropic.NewDefaultProvider()\nif err != nil {\n    // handle error\n}\n\nmodel := llm.NewModel(provider, \"claude-3-5-sonnet-latest\")\n</code></pre>"},{"location":"guides/providers/anthropic/#3-advanced-features","title":"3. Advanced Features","text":""},{"location":"guides/providers/anthropic/#thinking-reasoning-mode","title":"Thinking (Reasoning) Mode","text":"<p>Anthropic models support a dedicated \"thinking\" budget for extended reasoning.</p> <pre><code>model = model.WithThinking(4000) // 4000 tokens for internal reasoning\n</code></pre>"},{"location":"guides/providers/anthropic/#adaptive-thinking","title":"Adaptive Thinking","text":"<p>Enable the model to dynamically adjust its thinking process.</p> <pre><code>model = model.WithAdaptiveThinking()\n</code></pre>"},{"location":"guides/providers/anthropic/#prompt-caching","title":"Prompt Caching","text":"<p>Anthropic support \"Prompt Caching\" via ephemeral markers. Loom provides a structured way to leverage this at two levels:</p>"},{"location":"guides/providers/anthropic/#1-caching-the-header-call-extension","title":"1. Caching the \"Header\" (Call Extension)","text":"<p>This is the most common pattern. It caches the static parts of your request (system prompt and tool definitions) so they are reused across turns.</p> <pre><code>// Use the PromptCaching extension\nmodel = model.WithExtension(loomanthropic.PromptCaching{\n    CacheHeader: true,\n})\n</code></pre>"},{"location":"guides/providers/anthropic/#2-manual-breakpoints-message-extension","title":"2. Manual Breakpoints (Message Extension)","text":"<p>You can mark specific messages in the conversation as cache checkpoints using Message Extensions.</p> <pre><code>// Add a breakpoint to this specific turn using the fluent API\nmsg := message.NewUserText(\"Analyze this 500-page PDF...\").\n    WithExtension(&amp;loomanthropic.MessageCache{Enabled: true})\n</code></pre>"},{"location":"guides/providers/anthropic/#4-model-catalog","title":"4. Model Catalog","text":"<p>The provider maintains a list of known Anthropic models and their characteristics.</p> <pre><code>profile, found := provider.GetProfile(\"claude-3-5-sonnet-latest\")\nif found {\n    fmt.Println(\"Context Limit:\", profile.Limits.Context)\n}\n</code></pre>"},{"location":"guides/providers/google/","title":"Google Gemini Provider \ud83d\udd0c","text":"<p>The Google Gemini provider (GenAI) supports Gemini 1.5 Pro, Flash, and Gemini 2.0 models.</p>"},{"location":"guides/providers/google/#1-setup-authentication","title":"1. Setup &amp; Authentication","text":"<p>Set the <code>GOOGLE_API_KEY</code> environment variable.</p> <pre><code>export GOOGLE_API_KEY='your-api-key-here'\n</code></pre>"},{"location":"guides/providers/google/#2-basic-usage","title":"2. Basic Usage","text":"<pre><code>import (\n    \"context\"\n    \"github.com/masterkeysrd/loom/llm\"\n    \"github.com/masterkeysrd/loom/llm/genai\"\n)\n\nctx := context.Background()\nprovider, err := loomgenai.NewDefaultProvider(ctx)\nif err != nil {\n    // handle error\n}\n\nmodel := llm.NewModel(provider, \"gemini-1.5-pro\")\n</code></pre>"},{"location":"guides/providers/google/#3-advanced-features","title":"3. Advanced Features","text":""},{"location":"guides/providers/google/#reasoning","title":"Reasoning","text":"<p>Configure thinking levels for models that support it.</p> <pre><code>model = model.WithThinking(2000)\n</code></pre>"},{"location":"guides/providers/google/#context-caching","title":"Context Caching","text":"<p>Google Gemini supports long-lived context caches for large datasets or documents. Unlike Anthropic's implicit markers, Gemini caches must be explicitly created and referenced by ID.</p>"},{"location":"guides/providers/google/#1-creating-a-cache","title":"1. Creating a Cache","text":"<p>You can use <code>model.CreateCache</code> to upload content and get a cache ID.</p> <pre><code>cacheID, _ := model.\n    WithExtension(loomgenai.CacheCreation{\n        DisplayName: \"Project Docs\",\n        TTL:         1 * time.Hour,\n    }).\n    CreateCache(ctx, documentMessages)\n</code></pre>"},{"location":"guides/providers/google/#2-using-the-cache","title":"2. Using the Cache","text":"<p>Pass the ID back using the <code>ContextCache</code> extension.</p> <pre><code>resp, _ := model.Invoke(ctx, prompt, llm.WithExtensionOption(loomgenai.ContextCache{ID: cacheID}))\n</code></pre>"},{"location":"guides/providers/google/#3-deleting-the-cache","title":"3. Deleting the Cache","text":"<p>It is good practice to delete the cache when finished to save on costs.</p> <pre><code>model.DeleteCache(ctx, cacheID)\n</code></pre>"},{"location":"guides/providers/google/#4-model-profiles","title":"4. Model Profiles","text":"<p>Google provider models are statically defined and include information about multimodal support and context window sizes.</p> <pre><code>profiles := provider.SearchProfiles(\"gemini-1.5\")\n</code></pre>"},{"location":"guides/providers/ollama/","title":"Ollama Provider \ud83d\udd0c","text":"<p>The Ollama provider allows you to run open-source models (like Llama 3, Mistral, Qwen) locally on your machine.</p>"},{"location":"guides/providers/ollama/#1-setup","title":"1. Setup","text":"<p>Ollama must be installed and running on your system. You can download it from ollama.com.</p> <p>By default, Loom connects to the standard Ollama API at <code>http://localhost:11434</code>.</p>"},{"location":"guides/providers/ollama/#2-basic-usage","title":"2. Basic Usage","text":"<pre><code>import (\n    \"github.com/masterkeysrd/loom/llm\"\n    \"github.com/masterkeysrd/loom/llm/ollama\"\n)\n\nprovider, err := loomollama.NewDefaultProvider()\nif err != nil {\n    // handle error\n}\n\n// Make sure you have pulled the model locally: ollama pull llama3.1\nmodel := llm.NewModel(provider, \"llama3.1\")\n</code></pre>"},{"location":"guides/providers/ollama/#3-local-model-catalog","title":"3. Local Model Catalog","text":"<p>Unlike cloud providers, Ollama's available models depend on what you have installed locally. You can register your local models with Loom to ensure context window metadata is available:</p> <pre><code>provider.OverrideProfile(\"my-local-llama\", llm.ModelProfile{\n    ID: \"my-local-llama\",\n    DisplayName: \"Local Llama 3\",\n    Limits: llm.ModelLimits{\n        Context: 8192,\n    },\n})\n</code></pre>"},{"location":"guides/providers/openai/","title":"OpenAI Provider \ud83d\udd0c","text":"<p>The OpenAI provider allows you to use all standard GPT models, including reasoning models like <code>o1</code> and <code>o3-mini</code>, as well as standard models like <code>gpt-4o</code>.</p>"},{"location":"guides/providers/openai/#1-installation","title":"1. Installation","text":"<p>Ensure you have the Loom package installed:</p> <pre><code>go get github.com/masterkeysrd/loom\n</code></pre>"},{"location":"guides/providers/openai/#2-setup-authentication","title":"2. Setup &amp; Authentication","text":"<p>The OpenAI provider uses the <code>OPENAI_API_KEY</code> environment variable for authentication by default.</p> <pre><code>export OPENAI_API_KEY='your-api-key-here'\n</code></pre>"},{"location":"guides/providers/openai/#3-basic-usage","title":"3. Basic Usage","text":"<pre><code>import (\n    \"github.com/masterkeysrd/loom/llm\"\n    \"github.com/masterkeysrd/loom/llm/openai\"\n)\n\n// Create the provider\nprovider, err := loomopenai.NewDefaultProvider()\nif err != nil {\n    // handle error\n}\n\n// Instantiate a model\nmodel := llm.NewModel(provider, \"gpt-4o\")\n</code></pre>"},{"location":"guides/providers/openai/#4-advanced-features","title":"4. Advanced Features","text":""},{"location":"guides/providers/openai/#reasoning-models-thinking","title":"Reasoning Models (Thinking)","text":"<p>For models like <code>o1</code> or <code>o3-mini</code> that support internal reasoning, you can configure the thinking effort.</p> <pre><code>model = model.WithThinkingEffort(\"medium\") // low, medium, high\n</code></pre>"},{"location":"guides/providers/openai/#structured-output","title":"Structured Output","text":"<p>OpenAI's native JSON Schema enforcement is supported.</p> <pre><code>model = model.WithStructuredOutput(myJsonSchema)\n</code></pre>"},{"location":"guides/providers/openai/#prompt-caching","title":"Prompt Caching","text":"<p>OpenAI automatically caches prompt prefixes longer than 1024 tokens. Loom allows you to optimize this behavior using the <code>PromptCache</code> extension:</p>"},{"location":"guides/providers/openai/#1-cache-bucketing-key","title":"1. Cache Bucketing (Key)","text":"<p>Use a stable identifier to improve cache hit rates by bucketing similar requests together.</p> <pre><code>model = model.WithExtension(loomopenai.PromptCache{\n    Key: \"user-session-123\",\n})\n</code></pre>"},{"location":"guides/providers/openai/#2-extended-retention","title":"2. Extended Retention","text":"<p>By default, caches are ephemeral. You can enable extended caching (up to 24 hours) for specific prefixes.</p> <pre><code>model = model.WithExtension(loomopenai.PromptCache{\n    Retention: \"24h\",\n})\n</code></pre>"},{"location":"guides/providers/openai/#5-model-catalog","title":"5. Model Catalog","text":"<p>The provider includes a static catalog of common OpenAI models with their context limits and capabilities. You can search or list them at runtime:</p> <pre><code>profiles := provider.ListProfiles()\nfor _, p := range profiles {\n    fmt.Printf(\"Model: %s, Context: %d\\n\", p.ID, p.Limits.Context)\n}\n</code></pre>"}]}